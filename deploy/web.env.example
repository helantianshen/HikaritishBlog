NODE_ENV=production
HOSTNAME=127.0.0.1
PORT=3000

# Next SSR 读取公开数据；Next rewrite 也转发同源管理 API。
API_INTERNAL_URL=http://127.0.0.1:8080
API_PROXY_URL=http://127.0.0.1:8080

# 可选第三方集成，只存在于 Next 服务端。
GEMINI_API_KEY=
GITHUB_CLIENT_SECRET=
